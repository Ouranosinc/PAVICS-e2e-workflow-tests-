FROM pavics/workflow-tests:210216

RUN conda update -c zeitsperre -c conda-forge -c cdat -c bokeh -c plotly -c defaults -n birdy clisops xclim

USER root

RUN apt-get update && \
    DEBIAN_FRONTEND=noninteractive apt-get install -y fonts-humor-sans && \
    apt-get clean

USER jenkins
